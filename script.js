//Selectors
const addListBtn = document.querySelector('.new-list-btn');
const todoList = document.querySelector('.todo-list');
const listTitle = document.querySelector('.list-title');
const addItemBtn = document.querySelector('.new-todo-btn');
const inputTitle = document.querySelector('.title-entry');
const inputItem = document.querySelector('.todo-item');

// Functions
function addList() {}

//Event Listeners
addListBtn.addEventListener('click', addList);
// addItemBtn.addEventListener('click', addItem);
